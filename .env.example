# =============================================================================
# PORTFÓLIO SÉRGIO SANTOS — ENVIRONMENT VARIABLES TEMPLATE (.env.example)
# =============================================================================
#
# INSTRUÇÕES IMPORTANTES:
# -----------------------------------------------------------------------------
# 1. Copie este arquivo para: .env.local
# 2. Preencha APENAS o arquivo .env.local com valores reais.
# 3. O arquivo .env.local NÃO deve ser versionado (está no .gitignore).
# 4. NUNCA coloque valores sensíveis neste arquivo (.env.example).
#
# Este arquivo existe apenas como DOCUMENTAÇÃO e PADRÃO DE CONFIGURAÇÃO.
# =============================================================================


# =============================================================================
# MONITORAMENTO & TELEMETRIA (SERVER-SIDE ONLY)
# =============================================================================
#
# ⚠️ Estas variáveis NUNCA devem usar o prefixo NEXT_PUBLIC_
# ⚠️ Elas são acessíveis apenas no servidor (middleware, server components, APIs)

# Token do Logtail (Better Stack) para logs do middleware.ts
# Usado para observabilidade e auditoria de acessos.
# Obrigatório apenas se o monitoramento estiver habilitado.
#
# Obtenha em: https://betterstack.com/logtail
#
# Ambiente recomendado no Vercel:
# → Production ONLY
LOGTAIL_TOKEN=


# =============================================================================
# SEO, METADATA & DOMÍNIO CANÔNICO (CLIENT-SIDE)
# =============================================================================
#
# Variáveis com prefixo NEXT_PUBLIC_ são expostas ao browser.
# Use APENAS para valores que NÃO são sensíveis.

# URL oficial do portfólio
# Usada para:
# - metadata.ts (SEO)
# - sitemap.xml
# - robots.txt
# - links absolutos
#
# Exemplo:
# https://portfoliosergiosantos.vercel.app
#
# Ambiente recomendado no Vercel:
# → All Environments
NEXT_PUBLIC_SITE_URL=


# =============================================================================
# DATA SOURCE — GITHUB API (SERVER-SIDE ONLY)
# =============================================================================
#
# Token de Acesso Pessoal (PAT) do GitHub
# Usado para:
# - Evitar rate limit (403 Forbidden)
# - Buscar repositórios dinamicamente
# - Melhorar estabilidade em produção
#
# Escopo mínimo recomendado:
# → public_repo
#
# Obtenha em:
# GitHub > Settings > Developer Settings > Personal Access Tokens
#
# ⚠️ IMPORTANTE:
# - NÃO usar prefixo NEXT_PUBLIC_
# - NÃO expor no client
#
# Ambiente recomendado no Vercel:
# → Production ONLY
GITHUB_ACCESS_TOKEN=


# =============================================================================
# ANALYTICS & PERFORMANCE (CLIENT-SIDE) — OPCIONAL
# =============================================================================
#
# Ative apenas se realmente for usar analytics.

# Google Analytics 4 — Measurement ID
# Exemplo:
# G-XXXXXXXXXX
#
# Ambiente recomendado no Vercel:
# → Production (ou All Environments se necessário)
NEXT_PUBLIC_GA_ID=


# =============================================================================
# OBSERVAÇÕES FINAIS
# =============================================================================
#
# ✔ Este arquivo NÃO deve conter valores reais
# ✔ Use o Dashboard do Vercel como fonte de verdade em produção
# ✔ Prefira Production-only para tokens e segredos
# ✔ NEXT_PUBLIC_* = exposto ao browser
# ✔ Sem prefixo = server-side apenas
#
# =============================================================================
